import Tag from '../Tag';
import Title from './Title';
export default function HeroSection() {
  return (
    <section>
      <Tag text="[: android app" square={true} />
      <Title />
    </section>
  );
}
